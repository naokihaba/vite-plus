# command_env_list_remote_closed_pipe

## `vpt head-lines 1 -- vp env list-remote --lts`

Closing stdout after the heading does not make remote version listing fail

```
Node.js
note: Run `vp env clean` to free disk space from unused managed runtimes and package manager caches.
```
